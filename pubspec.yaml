name: austins
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.0.0 
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.10.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/icons/r.png

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
